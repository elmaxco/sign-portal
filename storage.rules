rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All client access is blocked. Attachments are handled via server APIs.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
